{
  "name": "financial-planner-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "migrate:test": "dotenv -e .env.test -- node scripts/migrate.js",
    "reset:test": "dotenv -e .env.test -- node scripts/reset.js",
    "seed:test": "dotenv -e .env.test -- node scripts/seed.js",
    "prepare:test": "npm run reset:test && npm run migrate:test && npm run seed:test",
    "test": "cross-env ENV_FILE=.env.test NODE_ENV=test vitest run",
    "test:watch": "cross-env ENV_FILE=.env.test NODE_ENV=test vitest",
    "test:all": "npm run prepare:test && npm test",
    "test:prepare": "npm run prepare:test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^2.110.7",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.11.0",
    "multer": "^2.2.0",
    "pg": "^8.22.0",
    "uuid": "^14.0.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "dotenv-cli": "^11.0.0",
    "nodemon": "^3.1.14",
    "supertest": "^7.2.2",
    "vitest": "^4.1.10"
  }
}
